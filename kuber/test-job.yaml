apiVersion: batch/v1
kind: Job

metadata:
  name: test-job

spec:
  template:
    spec:
      containers:
        - name: test
          image: busybox:1.36
          command:
            - sh
            - -c
            - "echo Starting job; sleep 10; echo Job completed"

      restartPolicy: Never
